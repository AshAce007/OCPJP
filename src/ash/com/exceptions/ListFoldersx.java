package ash.com.exceptions;

import java.io.File;
import java.util.Arrays;
import java.util.List;

public class ListFoldersx {
	/*public List<String> listAllFolders() {
		List<File> roots = Arrays.asList(File.listRoots());
		roots.forEach(rootDir -> );
		return null;
	}
	
	public List<String> getAllFolders(String root) {
		File parent = new File(root);
		List<File> childNodes = Arrays.asList(parent.listFiles());
		childNodes.
	}*/
}
